# mini-language-server

A language-independent language-server and compiler-tooling laboratory focused on versioning, stale-result suppression, cancellation, and deterministic semantic publication.

The repository has moved well beyond the original JSON-RPC bootstrap. Its stable tooling chain is:

```text
LSP / JSON-RPC lifecycle
        -> document snapshots + incremental edits
        -> syntax snapshots
        -> symbol index
        -> semantic references
        -> diagnostics + navigation/rename
        -> workspace indexing + cross-file Nova tooling
        -> cancellation + stale-result suppression
```

## Current capabilities

- bounded LSP `Content-Length` framing with fail-fast per-line, total-header-byte, header-count, and payload limits, plus JSON-RPC lifecycle handling with notification-only `initialized`/`exit` shape enforcement and idempotent terminal exit semantics
- executable cancellation-aware stdio host (`mini-language-server` and `python -m mini_language_server`) with one active client-request worker and a foreground protocol lane: `didOpen`/`didChange`/`didClose` plus negotiated `workspace/didChangeWorkspaceFolders`, `workspace/didChangeConfiguration`, `workspace/didRenameFiles`, and successfully registered `workspace/didChangeWatchedFiles` can advance exact document, workspace-scope, URI-identity, detached-filesystem, or save-formatting configuration generations while a request is still computing, queued client requests remain serial, the first valid shutdown request can interrupt and retire the active worker only across deferred ordinary client requests while preserving earlier server-response and live-mutation causality, its own response is held until that cancellation completes, `exit`/repeated lifecycle traffic and other deferred protocol traffic remain FIFO-ordered, valid responses to already-delivered pending server requests can reach an active worker through a guarded foreground lane without accepting guessed unsent ids, and an active worker that creates a new tracked server request can wake the outbound lane after consumer ownership and tracing are atomically published so that request is framed immediately and its response can resume the same worker; the dedicated framing reader stages `$/cancelRequest` against an already-read pending request generation, outbound notifications/server-requests drain before direct responses, wakeups coalesce on empty-to-non-empty outbox transitions, and malformed framing, stdin EOF/read failure, or stdout write/flush failure before `exit` fail closed through one terminal transport-abort boundary that cancels/pre-cancels the active request generation, retires pending server requests locally, closes notification ownership, suppresses post-failure worker output and inbound dispatch, detaches the transport wakeup, and returns non-zero
- standard LSP `$/setTrace` (`off` / `messages` / `verbose`) with product-wide `$/logTrace` lifecycle observability for client traffic, outbound notifications/progress, and tracked server-to-client requests; verbose mode records deterministic structural metadata without copying source text or arbitrary payload values
- document open/change/close with monotonic versions and incremental edits
- negotiated LSP position encoding across the complete session (`utf-8`, `utf-16`, and `utf-32`, defaulting compatibly to `utf-16`), with one coordinate contract for incremental edits, navigation/diagnostic/formatting ranges, workspace edits, and semantic-token starts/lengths
- version-bound syntax, symbol, semantic, diagnostic, and workspace snapshots
- compare-and-commit publication guards across the derived-cache chain, including exact open-document and diagnostic snapshot-set guards for workspace reads
- definition, references, document highlights, linked editing ranges, prepareRename, and rename, including uniquely resolved cross-file Nova functions; rename negotiates versioned `WorkspaceEdit.documentChanges` when supported so every edited URI carries the exact captured document version, adds semantic `changeAnnotations` when the client supports annotated edits, while legacy clients keep `changes`, and the final Nova product rejects workspace function-name collisions before publishing edits
- hover, lexical-scope-aware completion, semantic tokens (full, range, and negotiated full/delta) whose Nova delta result lineage is bound to the complete exact semantic workspace, with coalesced `workspace/semanticTokens/refresh` after cross-file semantic workspace changes, negotiated flat/hierarchical document symbols with Nova function-member ownership, workspace symbol search with exact-commit `partialResultToken` streaming and negotiated `workDoneToken` progress, negotiated signature help, negotiated Nova parameter inlay hints with coalesced `workspace/inlayHint/refresh` after cross-file workspace identity changes, exact-snapshot Nova function folding ranges, negotiated Nova lexical selection ranges, negotiated Nova whole-document formatting, negotiated Nova range formatting, negotiated Nova on-type indentation, negotiated exact-workspace Nova call hierarchy, and negotiated exact-workspace Nova reference-count CodeLens
- negotiated push-diagnostic metadata: `textDocument.publishDiagnostics.versionSupport` controls versioned push notifications and `tagSupport.valueSet` filters rendered LSP diagnostic tags without erasing tags from exact internal snapshots; the same tag renderer is reused by push, pull, and code-action diagnostic copies
- negotiated `textDocument/diagnostic` and `workspace/diagnostic` pull reports with deterministic result IDs, unchanged-report support, cancellation, direct cross-file `relatedDocuments` reports for single-document pulls, exact diagnostic-snapshot publication guards, complete scoped open-document membership guards, and pull-only `version: null` reports for detached closed Nova files using bounded duplicate-declaration diagnostics plus exact combined-workspace unresolved/ambiguous function resolution, uniquely resolved argument-count validation, and literal Int/String/Bool argument-type validation; standard `partialResultToken` support that streams document related-dependency reports or workspace report chunks only after the corresponding exact commit boundary, and negotiated `workDoneToken` begin/report/end status progress with cancellation/stale completion; related document reports are rendered from exact dependency snapshots and rejected as a unit on generation drift, while the generic server-to-client request substrate tracks outbound JSON-RPC requests/responses, and clients advertising `workspace.diagnostics.refreshSupport` receive coalesced `workspace/diagnostic/refresh` requests only after a cross-file workspace diagnostic transition wins its exact compare-and-commit
- standard `workspace/configuration` integration for save-time Nova formatting: after `initialized`, supporting clients receive one tracked request containing a global `mini-language-server.formatting` fallback plus deterministic `scopeUri` items for active workspace folders; clients that also advertise dynamic `workspace/didChangeConfiguration` registration receive one tracked `client/registerCapability` request for the formatting section before the initial configuration pull; when folder/configuration generation changes, an obsolete configuration request is retired immediately—locally if unsent or via standard `$/cancelRequest` if already delivered—and one fresh generation request is queued without waiting for a stale response; successful server shutdown retires every remaining tracked client request through the same consumer cleanup boundary, while exit performs local-only final retirement, so late responses cannot mutate formatting or registration state after lifecycle termination; `willSaveWaitUntil` uses the most-specific folder's validated `tabSize`/`insertSpaces`, publishes only while its exact semantic/workspace/configuration generations remain current, out-of-scope documents use the global fallback, client errors preserve the last valid settings, and clients without configuration support retain the deterministic four-space default
- negotiated workspace-folder scoping for cross-file Nova tooling: `workspaceFolders` with `rootUri` fallback bounds the shared semantic workspace, RFC-safe canonical URI identity normalizes scheme/host case and unreserved percent-encoding without decoding reserved path delimiters or guessing filesystem case rules, and configured local `file:` roots additionally feed a bounded detached closed-file index (UTF-8 `*.nova`, 2 MiB/file, 2048 files/scan) without polluting live document/syntax/semantic/diagnostic ownership; detached base diagnostics remain outside the live DiagnosticStore and are rendered only through exact-workspace pull diagnostics; open buffers override equivalent disk identities, `didClose` restores disk content, folder changes plus negotiated `workspace/didCreateFiles`, `workspace/didDeleteFiles`, and `workspace/didRenameFiles` reconcile the detached index, and clients supporting dynamic watched-file registration receive one tracked `workspace/didChangeWatchedFiles` `**/*.nova` create/change/delete subscription whose successful notifications trigger the same bounded rescan as live foreground snapshot mutations; workspace/search/navigation/call-hierarchy/rename consume the combined exact server-known workspace, and source-mutating rename revalidates detached disk text before publishing edits
- negotiated `workspace/willRenameFiles` + `workspace/didRenameFiles` support forms a two-phase resource-operation lifecycle across the complete server-known Nova workspace: preflight captures canonical affected URI identities plus exact open-document, combined workspace, folder-generation, and detached-file state, rejects duplicate canonical destinations and unrelated open/detached destination collisions, rereads detached sources before publication, honors cancellation, and returns `Content modified` on document/workspace/folder/disk drift; didRename performs one atomic open-document URI rekey, preserves text/version while creating new exact document identities, discards old URI-derived caches, rebuilds destination semantics, re-evaluates workspace scope, clears old push diagnostics, and triggers a bounded closed-file rescan; closed-file symbol rename uses `version: null` rather than a fabricated document version and rereads detached inputs before publication, while permission-aware filesystem transactions, OS-native/remote watcher providers, and module/import rewrite semantics remain outside this milestone
- executable Nova adapter semantics for functions, typed parameters, explicitly typed locals, locals, scoped references, return statements, and deterministic unresolved/duplicate diagnostics; the final product lexically masks line comments, block comments, and quoted strings before structural scans while preserving exact source offsets
- exact-workspace Nova hover type information for explicitly typed parameters, explicitly typed locals, literal-initialized locals, exact-reference local aliases, bounded uniquely resolved direct function-call initializers, bounded integer-arithmetic-initialized locals, bounded comparison-initialized locals, and bounded logical-expression-initialized locals when type knowledge is available; expression-derived local knowledge also feeds lexical completion details and local type inlay hints, inferred local hints carry exact zero-width `textEdits` that materialize `: Type` annotations, explicit typed locals remain display-only to avoid duplicate annotations, and mixed/unknown expressions remain conservative
- conservative Nova function-result inference for uniquely resolved unannotated functions whose reachable value-bearing returns consistently resolve to one bounded Int, String, or Bool type through literals, exact semantic parameter/local references with bounded explicit type knowledge, local alias chains rooted in bounded direct function-call initializers, acyclic chains of uniquely resolved direct calls, or bounded integer arithmetic/comparison/logical expressions whose operands resolve through the same exact semantic/workspace snapshot; return evidence is filtered through the same recursive semantic unreachable-region query used by `nova.unreachable-code`, so constant-dead branches and suffixes after returns, proven non-fallthrough loops, loop control, explicit/inferred never calls, and effect-aware aggregate termination cannot poison Int/String/Bool, Unit, or tail inference; unknown control-flow, reachable or possibly reachable loop exits, ambiguous targets, cycles, and conflicting/unsupported/unknown reachable returns remain conservative, and explicit return annotations are never overridden
- exact-workspace Nova completion details for bounded parameter/local types and uniquely resolved same-file or cross-file function signatures; negotiated `CompletionList.itemDefaults.editRange` shares one exact cursor replacement range across the list (including insert/replace ranges, snippets via `textEditText`, numeric intrinsics, and UTF-8/UTF-16/UTF-32 position units) while unnegotiated clients retain the existing item-array shape; negotiated LSP 3.18 `textDocument/inlineCompletion` reuses the same typed lexical candidate evidence for plain identifier prefixes with exact semantic/workspace publication, deterministic invoked/automatic selection, negotiated position units, and fail-closed specialized member/conversion contexts; ambiguous function names remain conservative
- negotiated `textDocument/inlineValue` debugger integration returns exact-snapshot `InlineValueVariableLookup` records for lexically visible Nova parameters and locals within the requested viewport; stopped-location ownership reuses the existing function-scope model, local shadowing mirrors semantic resolution, duplicate locals fail closed, negotiated position units are preserved, and cancellation or semantic drift rejects publication rather than exposing stale runtime lookup identities
- exact-workspace Nova call diagnostics, including unresolved/ambiguous functions, argument-count mismatches, and bounded argument-type checks for Int, String, and Bool literals plus exact semantic references to explicitly typed parameters, explicitly typed locals, literal-initialized locals, and local aliases whose initializer resolves through the same exact semantic snapshot; ambiguous-function diagnostics carry exact cross-file related locations for every candidate declaration when negotiated, and each related location is freshness-bound to its target semantic snapshot
- bounded Nova integer arithmetic expression typing propagates Int through `+`, `-`, `*`, `/`, and `%` when every operand resolves to Int from the exact semantic/workspace snapshot; mixed or unknown operands remain conservative, and the result participates in argument/return diagnostics and their existing quick fixes
- bounded Nova comparison expression typing produces Bool for `==`/`!=` when both operands resolve to the same bounded Int/String/Bool type, and for `<`/`<=`/`>`/`>=` when both operands resolve to Int; operands reuse exact-snapshot arithmetic/reference/function-call typing, while mixed, unknown, or chained comparisons remain conservative
- bounded Nova logical expression typing produces Bool for Bool-only `&&`/`||` operands and unary `!`, composing the existing exact-snapshot comparison/arithmetic/reference/function-call inference path; mixed, malformed, unknown, ambiguous, or cyclic inputs remain conservative, and the result participates in argument/return validation, function-result inference, and local tooling
- bounded Nova return validation reports deterministic `nova.return-type` diagnostics when Int, String, or Bool literals, exact semantic references with bounded parameter/local type knowledge, or uniquely resolved same-file/cross-file function calls with bounded explicit or inferred result types conflict with an explicit function return annotation; `nova.missing-return` is suppressed when every reachable fallthrough path is closed by a top-level value return, a structurally complete `if`/`else if`/`else` chain whose reachable arms recursively return or diverge, or a provably true `while` with no reachable break. Unknown loop conditions and loops with reachable or possibly reachable breaks remain conservative, while nested returns continue to participate in return-type validation
- bounded Nova semantic call effects treat a uniquely resolved standalone call at the top level of the analyzed function or nested brace body as non-fallthrough evidence for `nova.unreachable-code`, value-return obligations, definite-initialization branch joins, and aggregate `if`/`else if`/`else` termination when the target is explicitly `-> !` or is an unannotated function conservatively proven never-returning from exact current control flow; inferred effects propagate transitively through acyclic uniquely resolved call chains, branch aggregation can mix returns, proven divergent loops, and never-call effects while preserving precise unreachable-diagnostic wording, explicit non-`!` annotations, syntactic returns, ambiguous targets, cycles without an independent divergence base, and compound-expression calls remain conservative
- bounded explicit-local validation reports deterministic `nova.local-type` diagnostics when an Int, String, or Bool annotation conflicts with a bounded literal/reference/function-call, integer arithmetic, comparison, or logical expression whose type is known from the exact current workspace snapshot; unknown or unsupported compound expressions remain conservative
- exact-workspace Nova call hierarchy resolves uniquely named functions and deterministically aggregates incoming callers and outgoing callees without guessing across ambiguous declarations
- exact-workspace Nova reference CodeLens counts uniquely resolved cross-file call sites and exposes an exact-workspace reference-location command while suppressing ambiguous or stale results; clients advertising `workspace.codeLens.refreshSupport` receive coalesced `workspace/codeLens/refresh` requests after cross-file workspace identity changes so visible reference counts are re-pulled, while old resolve tokens still fail exact-snapshot validation
- literal-aware Nova call parsing so quoted commas, parentheses, and escaped quotes do not corrupt argument boundaries
- negotiated Nova quick fixes for unresolved functions, unresolved local names, argument-count mismatches, argument-type mismatches, return-type mismatches, missing value returns, explicit-local initializer type mismatches, duplicate function/parameter/local declarations, and unsupported unary `+`; eager and lazy-resolved edits use versioned `WorkspaceEdit.documentChanges` when negotiated, add action-title `changeAnnotations` when supported, and preserve legacy `changes` otherwise, while unary-plus repair removes only the exact diagnosed operator and all repairs remain gated on exact diagnostic/workspace snapshots
- duplicate-declaration quick fixes deterministically choose collision-free Nova identifiers within the relevant function or declaration namespace and edit only the exact diagnosed declaration span; duplicate function/parameter/local diagnostics also carry same-snapshot related locations for the other conflicting declarations when the client negotiates diagnostic related-information support
- trivia-aware Nova formatting reindents structural braces while preserving non-leading source text; braces inside comments and quoted strings do not affect indentation, and responses are gated on the exact current semantic/document snapshot
- conservative Nova range formatting reindents only leading whitespace spans fully contained in the requested range, computes structural depth from the complete trivia-aware document view, and never publishes edits that escape the requested range; clients advertising LSP 3.18 `textDocument.rangeFormatting.rangesSupport` also receive `textDocument/rangesFormatting`, which validates all requested ranges against one exact snapshot and formats their eligible lines in one document scan without duplicate edits
- conservative on-type Nova formatting reindents only the current line when `}` is typed, using the same trivia-aware structural view and exact semantic/document publication guard
- push diagnostics with stale-notification suppression
- request cancellation and stale-document rejection, including terminal shutdown/exit retirement of all in-flight client requests so resumed workers can only observe cancellation after the session lifecycle closes; the outbound notification channel is terminally quiesced at the same boundary, dropping queued diagnostics/progress/trace traffic and rejecting late worker emissions while preserving only shutdown-time `$/cancelRequest` notifications required for already-delivered server requests
- deterministic concurrency regressions for same-version snapshot replacement, close/reopen, and out-of-order publication
- CI across Ubuntu, macOS, and Windows on Python 3.11 and 3.13

## Checkpoint scope

The generic tooling substrate remains language-independent. Nova-specific parsing, lexical trivia masking, name-resolution rules, typed function metadata, conservative cycle-safe bounded function-result inference from literals, exact semantic explicit typed references, local direct-call/alias chains, uniquely resolved direct-call chains, exact-snapshot bounded arithmetic/comparison/logical expressions, arithmetic/comparison/logical-derived local type surfacing, bounded integer arithmetic, comparison, and logical expression typing, explicit local-type annotations and bounded initializer validation including exact-workspace function/reference/arithmetic/comparison/logical result propagation, bounded return-type validation including exact-workspace function-call result propagation and conservative branch-complete `if`/`else if`/`else` return-path proof, trivia-aware exact-workspace function-call initializer inference, cross-file product behavior, rename-collision policy, duplicate-declaration repair policy, call-hierarchy ownership/routing, reference-CodeLens counting/command policy, diagnostics, quick-fix policy, parameter inlay hints, function-body folding rules, lexical selection-range ownership, document-formatting, range-formatting, and on-type indentation policy, literal-aware call parsing, bounded typed parameter/local propagation, and lexical completion visibility stay in the Nova adapter/product composition instead of leaking into the generic stores and query layer. Pull-diagnostic protocol composition is language-independent and reads exact DiagnosticSnapshots without changing Nova diagnostic policy; workspace pulls additionally pin the exact open-document and diagnostic snapshot sets through generic compare-and-commit guards. Semantic-token delta composition is language-independent and wraps the exact full-token publication boundary without adding Nova-specific token rules.

The project is still intentionally bounded rather than a complete production LSP or full Nova compiler front end. New slices should add executable semantics or protocol behavior with exact-snapshot regressions, not empty handlers, adapters, or scaffolding.

See [`docs/stability-checkpoint.md`](docs/stability-checkpoint.md) for the maintenance boundary.

## Development

```bash
python -m pip install -e '.[dev]'
pytest
ruff check .
mini-language-server
```

Every derived result must remain bound to the exact document/syntax/symbol/semantic generation that produced it. Late work must be rejected rather than overwrite a newer snapshot.
